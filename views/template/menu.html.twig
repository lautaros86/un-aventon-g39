<nav class="navbar navbar-static-top" style="margin-bottom: 0px;">
    <div class="container">
        <div class="navbar-header">
            <a href="/" class="navbar-brand">
                <p  style="color: #fff">
                    <img width="25" height="25" src="../../img/Logo.jpg" class="img-circle" class="img-responsive" alt="logo">
                    Un<b>Aventon</b>
                </p>
            </a>
        </div>
        <!-- /.navbar-collapse -->
                <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse pull-left" id="navbar-collapse">
          <ul class="nav navbar-nav">
            <li><a href="/viaje/listado">Viajes</a></li>
          </ul>
        </div>
        <!-- Navbar Right Menu -->
        <div class="navbar-custom-menu  pull-right">

            <ul class="nav navbar-nav">
                <!-- User Account Menu -->
                {% if session.get('autenticado') %}

                    <li class="dropdown notifications-menu" id="notifications-menu">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" aria-expanded="true">
                            <i class="fa fa-bell-o"></i>
                            <span class="label label-warning" id="cantNotif" hidden></span>
                        </a>
                        <ul class="dropdown-menu">
                            <li class="header">Historial de Notificaciones</li>
                            <li>
                                <!-- inner menu: contains the actual data -->
                                <ul class="menu" id="menuNotif">

                                </ul>
                            </li>
                            <li class="footer"><a href="#">Ver Todas</a></li>
                        </ul>
                    </li>
                    <li class="dropdown user user-menu">
                        <!-- Menu Toggle Button -->
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                            <!-- The user image in the navbar-->
                            <img src="{% if session.get('usuario').foto %}{{session.get('usuario').foto}}{% else %}/img/anonimo.png{% endif %}" class="user-image" alt="User Image">
                            <!-- hidden-xs hides the username on small devices so only the image appears. -->
                            <span class="hidden-xs">{{session.get('usuario').nombre}} {{session.get('usuario').apellido}}</span>
                        </a>
                        <ul class="dropdown-menu">
                            <!-- The user image in the menu -->
                            <li class="user-header">
                                <img src="{% if session.get('usuario').foto %}{{session.get('usuario').foto}}{% else %}/img/anonimo.png{% endif %}" class="img-circle" alt="User Image">

                                <p>
                                    {{session.get('usuario').nombre}} {{session.get('usuario').apellido}}
                                </p>
                            </li>
                            <!-- Menu Body -->
                            <li class="user-body">
                                <div class="row">
                                    <div class="col-xs-3 text-center">
                                        <a href="#">Viajes Pasajero</a>
                                        <label>15</label>
                                    </div>
                                    <div class="col-xs-3 text-center">
                                        <a href="#">Rep. Pasajero</a>
                                        <label>150</label>              
                                    </div>
                                    <div class="col-xs-3 text-center">
                                        <a href="#" class>Viajes Chofer</a>
                                        <label>2</label>                        
                                    </div>
                                    <div class="col-xs-3 text-center">
                                        <a href="#">Rep. Chofer</a>
                                        <label>8</label>                
                                    </div>
                                </div>
                                <!-- /.row -->
                            </li>
                            <!-- Menu Footer-->
                            <li class="user-footer">
                                <div class="pull-left">
                                    <a href="/perfil" class="btn btn-default btn-flat">Perfil</a>
                                </div>
                                <div class="pull-right">
                                    <a href="/cerrarsesion" class="btn btn-default btn-flat">Cerrar Sesión</a>
                                </div>
                            </li>
                        </ul>
                    </li>
                {% else %}
                    <li class="dropdown user user-menu">
                        <!-- Menu Toggle Button -->
                        <a href="/login" class="dropdown-toggle">
                            <!-- hidden-xs hides the username on small devices so only the image appears. -->
                            <span class="hidden-xs">Ingresar</span>
                        </a>
                    </li>

                    <li class="dropdown user user-menu">

                        <!-- Menu Toggle Button -->
                        <a href="/registro" class="dropdown-toggle">
                            <!-- hidden-xs hides the username on small devices so only the image appears. -->
                            <span class="hidden-xs">Registrarse</span>
                        </a>
                    </li>

                {% endif %}
            </ul>
        </div>
        <!-- /.navbar-custom-menu -->
    </div>
    <!-- /.container-fluid -->
</nav>